{% extends 'base.html.twig' %}

{% block title %}Formation index{% endblock %}

{% block body %}
<nav class="navbar navbar-expand-lg navbar-light" style="background-color: #e3f2fd;">
    <a class="navbar-brand" href="#"><b>Education</b></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link " href="{{ path('app_ecole_index') }}">Ecoles</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="{{ path('app_formation_index') }}">Formations</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_relations') }}">Relations</a>
            </li>
        </ul>
    </div>
</nav>
<br />
    <h1 style="text-align:center; color: #007FFF; font-family: Helvetica">Formation </h1>
    <br />

    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th scope="row">#</th>
                <th>Titre de formation</th>
                <th>Durée</th>
                <th>Date de debut</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for formation in formations %}
            <tr>
                <th scope="row">{{ formation.id }}</th>
                <td>{{ formation.titre }}</td>
                <td>{{ formation.duree }}</td>
                <td>{{ formation.dateDebut }}</td>
                <td>
                    <a href="{{ path('app_formation_show', {'id': formation.id}) }} " class="btn btn-info btn-sm">show</a>
                    <a href="{{ path('app_formation_edit', {'id': formation.id}) }}" class="btn btn-warning btn-sm">edit</a>
                    {{ include('formation/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

   <center> <a href="{{ path('app_formation_new') }}" class="btn btn-success">Create new</a></center>
{% endblock %}
