<form method="post" action="{{ path('app_ecole_delete', {'id': ecole.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="mt-4">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ecole.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
